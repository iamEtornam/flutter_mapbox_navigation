org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
# AGP 7.4.2 does not officially support compileSdk 34 (required by Maps SDK v11).
# This suppresses the warning; the build is otherwise compatible.
android.suppressUnsupportedCompileSdk=34
# MAPBOX_DOWNLOADS_TOKEN (a secret token with the `downloads:read` scope) is required
# to download the Mapbox SDK binaries. Do NOT commit it here. Put it in your global
# ~/.gradle/gradle.properties or export it as an environment variable instead:
#   MAPBOX_DOWNLOADS_TOKEN=sk.XXXXXXXX
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
